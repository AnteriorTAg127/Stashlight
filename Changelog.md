# Changelog

## [0.6.0-beta] - 2026-08-10

### Added
- **工作台合成（v1.5）**：搜索界面底部翻页栏新增「合成」页，基于原版配方书列出已解锁配方：
  - 配方列表 + 可合成/缺材料状态，支持**配方搜索**与**「仅显示可合成」开关**（与设置界面同步）。
  - 点击配方渲染 3x3（/2x2）合成预览，结果槽显示每批产出数量（如铁锭→铁粒 ×9）。
  - 详情区：数量输入、上限/每批、实时消耗表（需要/背包有/范围内有/需取）、**一键拿取并合成**。
  - 合成台自动判定：可触碰范围内有工作台 → 3x3 全配方；无 → 背包 2x2 合成格（原版机制，无服务端模组也可用）。
  - **一键拿取并合成**：材料缺失量自动入取队列 → modded/vanilla 双模取物 → 原版配方书方式放置 + shift 批量合成，目标精确停止不超量。
  - 合成页左上「周边库存」区：显示可触碰箱子内的物品，点击自动匹配以它为原料（或产物）的配方。
- 合成页底部横排布局：合成预览 | 合成操作 | 背包空间栏，垂直方向更省空间。
- **高级选项（默认关闭）**「允许丢弃多余材料并等待回收」：合成取物背包放不下时可将多余材料丢到地上，合成前检测周围掉落物、等待回收后再继续；掉落物无法自动拾取时可选「等待手动捡起」或「按现有材料部分合成」。
- 取物优化：vanilla 取物先合并到背包已有同物品堆叠再填空格（此前 45 个钻石需 45 个空格，空格不足会误丢材料）。
- 纯计算模块 `CraftMath` + 10 个单元测试（数量/批次/消耗/上限语义）。
- 内部重构：背包计数与可达判定抽取至 `Util`（`countInInventory` / `countInReach` / `isWithinReach` / `reachableItems`）。

### Fixed
- 合成取物结束后的延迟重开搜索界面不再覆盖玩家正在打开的其他界面（如设置界面）。
- 取物失败现在有明确提示：背包空间不足会记录原因；箱子中找不到目标物品时提示「索引可能过期或物品已被移走，请重新扫描」。
- 修复了从嵌套容器中远程取物时因目标 Stack 与实际槽位物品不匹配而失败的问题。
- 修复了合成数量统计：`done: X of target` 只计本次合成量（不再把合成前背包已有的产物计入）。
- 修复合成循环误停：逐批合成第一轮后因结果槽为空被误判结束（`done: 4 of 700`）。
- 修复批量合成超量：整组放置时产出可能超过目标（192 目标出 256），现按剩余量动态整组/逐批放置，精确命中目标。
- 修复合成台 NPE 崩溃（recover 模式下 `tablePos` 未初始化）。
- 合成目标按背包容量预判截断，避免产物装不下导致中途卡死；容量不足且背包有旧产物时自动丢弃旧产物腾位（随后自动捡回，不丢失物品）。

### Changed
- 版本号 0.5.0 → 0.6.0（mod_version 0.6.0-beta）。

---

## Unreleased（v1.4 未发布内容）

### Added
- 远程取物 / 原版服取物现在支持"优先取整盒"：当目标物品存放在潜影盒、Bundle 等嵌套容器里时，会按盒子内部实际目标物品数量计算，优先取走能装下的整盒，再用散装补齐；没有散装时允许多拿一整盒。
- 在"高亮设置"的远程取物卡片中新增"优先取出整盒（潜影盒/Bundle）"复选框。
- 新增"拿取队列"：在数量选择对话框中可将物品加入队列，队列容量可配置（默认 16）；超出范围或已不存在的条目会显示为灰色并在处理时自动跳过；支持 modded 与原版服两种模式，退出世界后自动清空。
- 搜索界面底部的背包栏现在支持悬停显示物品提示信息。
- 搜索栏位与背包栏悬停时按住 Shift 会以类似箱子的网格界面预览潜影盒、Bundle 等嵌套容器的内容物。

### Fixed
- 修复了从嵌套容器中远程取物时因目标 Stack 与实际槽位物品不匹配而失败的问题。
